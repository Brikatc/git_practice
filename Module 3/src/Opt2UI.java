import javax.swing.*;

public class Opt2UI extends JFrame {
    // initiate frame
    JFrame orangeApp;
    JPanel back = new JPanel();
    JLabel header;
    JButton dateTime;
    JButton Save;
    JButton orange;
    Color tangerine = new Color(255, 153, 0);
    JButton exit;

    Opt2UI() {
        orangeApp = new JFrame();
        orangeApp.add(back);

        // header and variables
    }
}
